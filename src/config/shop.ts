import { ShopConfig } from '../types/product';

export const SHOP_CONFIG: ShopConfig = {
    enableCart: false,
    enableCheckout: false,
    showPrices: false,
    contactMode: 'whatsapp',
    whatsappNumber: '523521681197' // The number from App.tsx
};
